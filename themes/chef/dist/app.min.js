/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, { enumerable: true, get: getter });
/******/ 		}
/******/ 	};
/******/
/******/ 	// define __esModule on exports
/******/ 	__webpack_require__.r = function(exports) {
/******/ 		if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 			Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 		}
/******/ 		Object.defineProperty(exports, '__esModule', { value: true });
/******/ 	};
/******/
/******/ 	// create a fake namespace object
/******/ 	// mode & 1: value is a module id, require it
/******/ 	// mode & 2: merge all properties of value into the ns
/******/ 	// mode & 4: return value when already ns object
/******/ 	// mode & 8|1: behave like require
/******/ 	__webpack_require__.t = function(value, mode) {
/******/ 		if(mode & 1) value = __webpack_require__(value);
/******/ 		if(mode & 8) return value;
/******/ 		if((mode & 4) && typeof value === 'object' && value && value.__esModule) return value;
/******/ 		var ns = Object.create(null);
/******/ 		__webpack_require__.r(ns);
/******/ 		Object.defineProperty(ns, 'default', { enumerable: true, value: value });
/******/ 		if(mode & 2 && typeof value != 'string') for(var key in value) __webpack_require__.d(ns, key, function(key) { return value[key]; }.bind(null, key));
/******/ 		return ns;
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = "./assets/js/app.js");
/******/ })
/************************************************************************/
/******/ ({

/***/ "./assets/js/app.js":
/*!**************************!*\
  !*** ./assets/js/app.js ***!
  \**************************/
/*! no exports provided */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var _js_autch__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../js/autch */ \"./assets/js/autch.js\");\n/* harmony import */ var _js_autch__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_js_autch__WEBPACK_IMPORTED_MODULE_0__);\n/* harmony import */ var _js_subscribe__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../js/subscribe */ \"./assets/js/subscribe.js\");\nvar html = document.querySelector(\"html\"),\n    bgCover = document.querySelector(\".cover-bg\"); // BURGER MENU\n\nvar burger = document.querySelector(\".header__burger\"),\n    menu = document.querySelector(\".header-menu\");\nburger.addEventListener(\"click\", function (e) {\n  e.preventDefault;\n  burger.classList.toggle(\"close\");\n  html.classList.toggle(\"stop-scrolling\");\n  menu.classList.toggle(\"active\");\n}); // FAQ PAGE\n\nvar qBx = document.querySelectorAll(\".faq__question-box\");\n\nfor (var i = 0; i < qBx.length; i++) {\n  qBx[i].addEventListener(\"click\", function (e) {\n    e.preventDefault;\n\n    if (!this.nextElementSibling.classList.contains(\"active\")) {\n      qBx.forEach(function (item) {\n        item.nextElementSibling.classList.remove(\"active\");\n        item.children[1].classList.remove(\"active\");\n      });\n      this.children[1].classList.add(\"active\");\n      this.nextElementSibling.classList.add(\"active\");\n    } else {\n      this.nextElementSibling.classList.remove(\"active\");\n      this.children[1].classList.remove(\"active\");\n    }\n  });\n} // LOGIN POPUP\n\n\nvar signInBtn = document.querySelectorAll(\".header__btn-sign-in\"),\n    login = document.querySelector(\".login-popup\"),\n    loginCloseBtn = document.querySelector(\".close-btn_login\");\n\nfor (var _i = 0; _i < signInBtn.length; _i++) {\n  signInBtn[_i].addEventListener(\"click\", openLogin);\n}\n\nloginCloseBtn.addEventListener(\"click\", closeLogin);\nbgCover.addEventListener(\"click\", closeLogin);\n\nfunction openLogin(e) {\n  e.preventDefault();\n  login.classList.add(\"active\");\n  bgCover.classList.add(\"active\");\n  html.classList.add(\"stop-scrolling\");\n}\n\nfunction closeLogin(e) {\n  e.preventDefault();\n  login.classList.remove(\"active\");\n  bgCover.classList.remove(\"active\");\n  html.classList.remove(\"stop-scrolling\");\n}\n\nvar loginEye = document.querySelector(\".login-popup__eye\"),\n    passInput = document.querySelector('.login-popup__input[type=\"password\"]'); // SIGN UP\n\nvar signUpBtn = document.querySelectorAll(\".header__btn-sign-up\"),\n    signUp = document.querySelector(\".register-popup\"),\n    signUpCloseBtn = document.querySelector(\".close-btn_register\");\n\nfor (var _i2 = 0; _i2 < signUpBtn.length; _i2++) {\n  signUpBtn[_i2].addEventListener(\"click\", openSignUp);\n}\n\nsignUpCloseBtn.addEventListener(\"click\", closeSignUp);\nbgCover.addEventListener(\"click\", closeSignUp);\n\nfunction openSignUp(e) {\n  e.preventDefault();\n  signUp.classList.add(\"active\");\n  bgCover.classList.add(\"active\");\n  html.classList.add(\"stop-scrolling\");\n}\n\nfunction closeSignUp(e) {\n  e.preventDefault();\n  signUp.classList.remove(\"active\");\n  bgCover.classList.remove(\"active\");\n  html.classList.remove(\"stop-scrolling\");\n}\n\nvar signUpEye = document.querySelectorAll(\".register-popup__eye\"),\n    signUpPassInput = document.querySelectorAll('.register-popup__input[type=\"password\"]'); // SHOW PASSWORD\n\n[\"mousedown\", \"mouseup\", \"touchstart\", \"touchend\"].forEach(function (event) {\n  loginEye.addEventListener(event, showPass, false);\n});\n[\"mousedown\", \"mouseup\", \"touchstart\", \"touchend\"].forEach(function (event) {\n  for (var _i3 = 0; _i3 < signUpEye.length; _i3++) {\n    signUpEye[_i3].addEventListener(event, showPass, false);\n  }\n});\n\nfunction showPass(e) {\n  e.preventDefault();\n\n  if (this.parentNode.children[1]) {\n    this.parentNode.children[1].type === \"password\" ? this.parentNode.children[1].type = \"text\" : this.parentNode.children[1].type = \"password\";\n  }\n} // SUBSCRIBE POPUP\n\n\nvar subscribePopup = document.querySelector(\".subscribe-popup\");\nvar closeSubscribeBtn = document.querySelector(\".close-btn_subscribe\"); // setTimeout(showSubscribePopup, 5000);\n// showSubscribePopup();\n\nvar bgCoverSubscribe = document.querySelector(\".bgCoverSubscribe\");\nif (bgCoverSubscribe) bgCoverSubscribe.addEventListener(\"click\", closeSubscribePopup);\nif (closeSubscribeBtn) closeSubscribeBtn.addEventListener(\"click\", closeSubscribePopup);\n\nfunction showSubscribePopup() {\n  subscribePopup.classList.add(\"active\");\n  bgCoverSubscribe.classList.add(\"active\");\n  html.classList.add(\"stop-scrolling\");\n}\n\nfunction closeSubscribePopup() {\n  subscribePopup.classList.remove(\"active\");\n  bgCoverSubscribe.classList.remove(\"active\");\n  html.classList.remove(\"stop-scrolling\");\n} // QUESTION POPUP\n\n\nvar qPopup = document.querySelector(\".question-popup\"),\n    closeBtnQ = document.querySelector(\".close-btn_question\"),\n    bgCoverQuestion = document.querySelector(\".bgCoverQuestion\"); // if (qPopup) setTimeout(showQPopup, 1000);\n\nif (bgCoverQuestion) bgCoverQuestion.addEventListener(\"click\", closeQePopup);\nif (closeBtnQ) closeBtnQ.addEventListener(\"click\", closeQePopup);\n\nfunction showQPopup() {\n  qPopup.classList.add(\"active\");\n  bgCoverQuestion.classList.add(\"active\");\n  html.classList.add(\"stop-scrolling\");\n}\n\nfunction closeQePopup() {\n  qPopup.classList.remove(\"active\");\n  bgCoverQuestion.classList.remove(\"active\");\n  html.classList.remove(\"stop-scrolling\");\n} // SINGLE PRODUCT SLIDER\n\n\nfunction initThumbs(direction) {\n  return new Swiper(\".product__slider-thumbs\", {\n    slidesPerView: 4,\n    freeMode: true,\n    watchSlidesVisibility: true,\n    watchSlidesProgress: true,\n    direction: direction\n  });\n}\n\nvar prodThumbs = window.innerWidth < 992 ? initThumbs(\"horizontal\") : initThumbs(\"vertical\");\nvar prodSlider = new Swiper(\".product__slider\", {\n  navigation: {\n    nextEl: \".product__slider-arrow_next\",\n    prevEl: \".product__slider-arrow_prev\"\n  },\n  thumbs: {\n    swiper: prodThumbs\n  },\n  effect: \"flip\",\n  lazy: true,\n  keyboard: {\n    enabled: true\n  },\n  pagination: {\n    el: \".product__slider-pagination\",\n    clickable: true\n  }\n}); // cart quantity\n\n$(document).ready(function () {\n  $(document).on(\"click\", \".cart-form__quantity-btn_plus\", function (e) {\n    e.preventDefault();\n    var inp = this.parentNode.children[1].children[1],\n        count = +inp.value,\n        max = inp.getAttribute(\"max\");\n\n    if (count == max) {\n      return;\n    } else {\n      count += 1;\n      inp.setAttribute(\"value\", count);\n    }\n\n    $(\".qty\").trigger(\"change\");\n    $('button[name=\"update_cart\"]').trigger(\"click\");\n  });\n  $(document).on(\"click\", \".cart-form__quantity-btn_minus\", function (e) {\n    e.preventDefault();\n    var inp = this.parentNode.children[1].children[1],\n        count = +inp.value,\n        min = inp.getAttribute(\"min\");\n\n    if (count == min) {\n      return;\n    } else {\n      count -= 1;\n      inp.setAttribute(\"value\", count);\n    }\n\n    $(\".qty\").trigger(\"change\");\n    $('button[name=\"update_cart\"]').trigger(\"click\");\n  });\n}); // single product quantity\n\n$(document).ready(function () {\n  $(document).on(\"click\", \".product-form__quantity-btn_plus\", function (e) {\n    e.preventDefault();\n    var inp = this.parentNode.children[1].children[1],\n        count = +inp.value,\n        max = inp.getAttribute(\"max\");\n\n    if (count == max) {\n      return;\n    } else {\n      count += 1;\n      inp.setAttribute(\"value\", count);\n    }\n\n    $(\".qty\").trigger(\"change\");\n    $('button[name=\"update_cart\"]').trigger(\"click\");\n  });\n  $(document).on(\"click\", \".product-form__quantity-btn_minus\", function (e) {\n    e.preventDefault();\n    var inp = this.parentNode.children[1].children[1],\n        count = +inp.value,\n        min = inp.getAttribute(\"min\");\n\n    if (count == min) {\n      return;\n    } else {\n      count -= 1;\n      inp.setAttribute(\"value\", count);\n    }\n\n    $(\".qty\").trigger(\"change\");\n    $('button[name=\"update_cart\"]').trigger(\"click\");\n  });\n}); //related slider single product page\n\nvar relatedSlider = new Swiper(\".related__slider\", {\n  navigation: {\n    nextEl: \".related__slider-arrow_next\",\n    prevEl: \".related__slider-arrow_prev\"\n  },\n  allowTouchMove: true,\n  breakpoints: {\n    550: {\n      slidesPerView: 2\n    },\n    768: {\n      slidesPerView: 3\n    },\n    992: {\n      slidesPerView: 4,\n      navigation: false,\n      allowTouchMove: false\n    }\n  }\n}); // homepage catalog slider single product page\n\nfunction homeProdsFn() {\n  if (document.body.classList.contains(\"home\")) {\n    var products = document.querySelector(\".products\"),\n        productsContent = products.innerHTML;\n    products.innerHTML = \"<div class='catalog-home__slider swiper-container'>\\n                            <div class='swiper-wrapper'>\\n                              \".concat(productsContent, \"\\n                            </div>\\n                            <div class='catalog-home__slider-arrow-box'>\\n                              <div class=\\\"swiper-button-prev catalog-home__slider-arrow catalog-home__slider-arrow_prev\\\">\\n                                <svg width=\\\"16\\\" height=\\\"24\\\" viewBox=\\\"0 0 16 24\\\" fill=\\\"none\\\" xmlns=\\\"http://www.w3.org/2000/svg\\\">\\n                                  <path d=\\\"M15 1L1 12.5L15 23\\\" stroke=\\\"white\\\"/>\\n                                </svg>\\n                              </div>\\n                              <div class=\\\"swiper-button-next catalog-home__slider-arrow catalog-home__slider-arrow_next\\\">\\n                                <svg width=\\\"16\\\" height=\\\"24\\\" viewBox=\\\"0 0 16 24\\\" fill=\\\"none\\\" xmlns=\\\"http://www.w3.org/2000/svg\\\">\\n                                  <path d=\\\"M15 1L1 12.5L15 23\\\" stroke=\\\"white\\\"/>\\n                                </svg>\\n                              </div>\\n                            </div>\\n                          </div>\");\n    $(\".products__card\").wrap(\"<div class='swiper-slide'></div>\");\n  } else {\n    return;\n  }\n}\n\nhomeProdsFn();\nvar homeCatalogSlider = new Swiper(\".catalog-home__slider\", {\n  navigation: {\n    nextEl: \".catalog-home__slider-arrow_next\",\n    prevEl: \".catalog-home__slider-arrow_prev\"\n  },\n  // slidesPerView: 1,\n  allowTouchMove: true,\n  breakpoints: {\n    550: {\n      slidesPerView: 2\n    },\n    768: {\n      slidesPerView: 3\n    },\n    992: {\n      slidesPerView: 4,\n      navigation: false,\n      allowTouchMove: false\n    }\n  }\n}); // popup checkout\n// let checkoutBtn = document.querySelector(\".checkout-button\"),\n//   checkout = document.querySelector(\".popup-checkout\");\n// if (checkoutBtn) {\n//   checkoutBtn.addEventListener(\"click\", checkoutFn);\n//   bgCover.addEventListener(\"click\", function () {\n//     html.classList.remove(\"stop-scrolling\");\n//     bgCover.classList.remove(\"active\");\n//     checkout.classList.remove(\"active\");\n//   });\n// }\n// function checkoutFn(e) {\n//   e.preventDefault();\n//   bgCover.classList.add(\"active\");\n//   html.classList.add(\"stop-scrolling\");\n//   checkout.classList.add(\"active\");\n// }\n\nvar checkoutBtn = $(\".checkout-button\"),\n    checkout = $(\".popup-checkout\");\n\nif (checkoutBtn) {\n  var checkoutFn = function checkoutFn(e) {\n    e.preventDefault();\n    $(\".cover-bg\").addClass(\"active\");\n    $(\"html\").addClass(\"stop-scrolling\");\n    checkout.addClass(\"active\");\n  };\n\n  // checkoutBtn.on(\"click\", checkoutFn);\n  $(document).on(\"click\", \".checkout-button\", checkoutFn);\n  $(\".cover-bg\").on(\"click\", function () {\n    $(\"html\").removeClass(\"stop-scrolling\");\n    $(\".cover-bg\").removeClass(\"active\");\n    checkout.removeClass(\"active\");\n  });\n} // insta\n\n\nvar insta = document.querySelector(\".insta\");\n\nif (insta) {\n  setTimeout(function () {\n    var instaArrows = document.querySelectorAll(\".sbi-owl-nav > div\");\n\n    for (var _i4 = 0; _i4 < instaArrows.length; _i4++) {\n      instaArrows[_i4].innerHTML = \"<svg width=\\\"16\\\" height=\\\"24\\\" viewBox=\\\"0 0 16 24\\\" fill=\\\"none\\\" xmlns=\\\"http://www.w3.org/2000/svg\\\">\\n                      <path d=\\\"M15 1L1 12.5L15 23\\\" stroke=\\\"white\\\"/>\\n                    </svg>\";\n    }\n\n    var instaLink = document.querySelector(\".sbi_follow_btn a\");\n    instaLink.classList.add(\"btn\");\n    instaLink.children[0].remove();\n  }, 100);\n} //single prod reviews\n\n\nvar allReviewsBtn = document.querySelector(\".reviews__show-reviews\"),\n    leaveFeedbackBtn = document.querySelector(\".reviews__leave-feedback\"),\n    allReviews = document.querySelector(\".allReviews\"),\n    leaveFeedbackPopup = document.querySelector(\".leaveFeedback\"),\n    allReviewsClose = document.querySelector(\".allReviews__close\"),\n    leaveFeedbackClose = document.querySelector(\".leaveFeedback__close\");\nif (allReviewsBtn) allReviewsBtn.addEventListener(\"click\", showReviews);\nif (leaveFeedbackBtn) leaveFeedbackBtn.addEventListener(\"click\", leaveFeedback);\nif (allReviewsClose) allReviewsClose.addEventListener(\"click\", closeAllReviews);\nif (bgCover) bgCover.addEventListener(\"click\", closeAllReviews);\nif (leaveFeedbackClose) leaveFeedbackClose.addEventListener(\"click\", closeLeaveFeedback);\nif (bgCover) bgCover.addEventListener(\"click\", closeLeaveFeedback); // bgCover.addEventListener(\"click\", closeAllReviews);\n// bgCover.addEventListener(\"click\", closeLeaveFeedback);\n\nfunction showReviews(e) {\n  e.preventDefault();\n  html.classList.add(\"stop-scrolling\");\n  allReviews.classList.add(\"active\");\n  bgCover.classList.add(\"active\");\n}\n\nfunction closeAllReviews() {\n  html.classList.remove(\"stop-scrolling\");\n  allReviews.classList.remove(\"active\");\n  bgCover.classList.remove(\"active\");\n}\n\nfunction leaveFeedback(e) {\n  e.preventDefault();\n  html.classList.add(\"stop-scrolling\");\n  leaveFeedbackPopup.classList.add(\"active\");\n  bgCover.classList.add(\"active\");\n}\n\nfunction closeLeaveFeedback() {\n  html.classList.remove(\"stop-scrolling\");\n  leaveFeedbackPopup.classList.remove(\"active\");\n  bgCover.classList.remove(\"active\");\n}\n\nvar nameFeedback = document.querySelector(\".comment-form-author input\"),\n    emailFeedback = document.querySelector(\".comment-form-email input\"),\n    commentFeedback = document.querySelector(\".comment-form-comment textarea\"),\n    submitFeedback = document.querySelector('.form-submit input[type=\"submit\"]');\nif (nameFeedback) nameFeedback.setAttribute(\"placeholder\", \"Name *\");\nif (emailFeedback) emailFeedback.setAttribute(\"placeholder\", \"Email *\");\nif (commentFeedback) commentFeedback.setAttribute(\"placeholder\", \"Comment\");\nif (nameFeedback) nameFeedback.classList.add(\"leaveFeedback__input\");\nif (emailFeedback) emailFeedback.classList.add(\"leaveFeedback__input\");\nif (commentFeedback) commentFeedback.classList.add(\"leaveFeedback__input\", \"leaveFeedback__textarea\");\nif (submitFeedback) submitFeedback.classList.add(\"popup-submit\", \"leaveFeedback__submit\");\n //\n\n$(document).on(\"added_to_cart updated_cart_totals removed_from_cart\", function (data) {\n  $.ajax({\n    url: wp.ajaxurl,\n    type: \"POST\",\n    data: {\n      action: \"cart_count\"\n    },\n    success: function success(response) {\n      $(\".header__cart-link span\").html(response.data);\n    }\n  });\n}); // search\n\nvar searchIcon = document.querySelector(\".header__search svg\"),\n    searchForm = document.querySelector(\".search-form\"),\n    searchClose = document.querySelector(\".search-form__close\");\nsearchIcon.addEventListener(\"click\", showSearchFrom);\nsearchClose.addEventListener(\"click\", hideSearchFrom);\n\nfunction showSearchFrom() {\n  searchForm.classList.add(\"is-active\");\n}\n\nfunction hideSearchFrom() {\n  searchForm.classList.remove(\"is-active\");\n} // phone validate\n\n\nvar tel = document.querySelector('input[type=\"tel\"]'); // let tel = document.getElementById(\"tel\");\n\nif (tel) {\n  var setInputFilter = function setInputFilter(textbox, inputFilter) {\n    [\"input\", \"keydown\", \"keyup\", \"mousedown\", \"mouseup\", \"select\", \"contextmenu\", \"drop\"].forEach(function (event) {\n      textbox.addEventListener(event, function () {\n        if (inputFilter(this.value)) {\n          this.oldValue = this.value;\n          this.oldSelectionStart = this.selectionStart;\n          this.oldSelectionEnd = this.selectionEnd;\n        } else if (this.hasOwnProperty(\"oldValue\")) {\n          this.value = this.oldValue;\n          this.setSelectionRange(this.oldSelectionStart, this.oldSelectionEnd);\n        } else {\n          this.value = \"\";\n        }\n      });\n    });\n  };\n\n  setInputFilter(tel, function (value) {\n    return /^\\d*$/.test(value);\n  });\n} // function phoneValidation(ph) {\n//   let regex = /^\\d{10}$/;\n//   if (ph.value.match(regex)) return 1;\n//   else console.log(\"Nope!\");\n// }\n// let test = document.querySelector(\".wpcf7-tel\");\n// phoneValidation(test);\n// cookies to close subscribe popup\n\n\n$(document).ready(function () {\n  if (!readCookie(\"hideSubscribe\")) {\n    showSubscribePopup();\n  }\n\n  $(\".close-btn_subscribe\").click(closeSubsc);\n  $(\".bgCoverSubscribe\").click(closeSubsc);\n});\n\nfunction closeSubsc() {\n  $(\"#subscribe-popup\").hide();\n  createCookie(\"hideSubscribe\", true, 1);\n  return false;\n}\n\nfunction createCookie(name, value, days) {\n  if (days) {\n    var date = new Date();\n    date.setTime(date.getTime() + days * 24 * 60 * 60 * 1000);\n    var expires = \"; expires=\" + date.toGMTString();\n  } else var expires = \"\";\n\n  document.cookie = name + \"=\" + value + expires + \"; path=/\";\n}\n\nfunction readCookie(name) {\n  var nameEQ = name + \"=\";\n  var ca = document.cookie.split(\";\");\n\n  for (var i = 0; i < ca.length; i++) {\n    var c = ca[i];\n\n    while (c.charAt(0) == \" \") {\n      c = c.substring(1, c.length);\n    }\n\n    if (c.indexOf(nameEQ) == 0) return c.substring(nameEQ.length, c.length);\n  }\n\n  return null;\n} // function eraseCookie(name) {\n//   createCookie(name, \"\", -1);\n// }\n// cookies to close questions popup\n\n\n$(document).ready(function () {\n  if (!readCookie(\"hideQuestion\")) {\n    showQPopup();\n  }\n\n  $(\".close-btn_question\").click(closeQuestion);\n  $(\".bgCoverQuestion\").click(closeQuestion);\n});\n\nfunction closeQuestion() {\n  $(\"#question-popup\").hide();\n  createCookie(\"hideQuestion\", true, 1);\n  return false;\n} ////////////////\n\n\n // lang switcher\n\nvar drop = document.querySelector(\".wpm-languages a[aria-current] \");\nvar dropMenu = document.querySelector(\".wpm-languages .sub-menu \");\ndrop.addEventListener(\"click\", function (e) {\n  e.preventDefault();\n  dropMenu.classList.toggle(\"active\");\n  drop.classList.toggle(\"active\");\n});\n\n//# sourceURL=webpack:///./assets/js/app.js?");

/***/ }),

/***/ "./assets/js/autch.js":
/*!****************************!*\
  !*** ./assets/js/autch.js ***!
  \****************************/
/*! no static exports found */
/***/ (function(module, exports) {

eval("jQuery(function ($) {\n  /**\r\n   * Autch Ajax\r\n   */\n  $(document).on(\"submit\", \"#autch_form_login\", function (e) {\n    var $this = $(this);\n    $.ajax({\n      type: \"POST\",\n      url: ajax_account_object.ajaxurl,\n      data: $this.serialize(),\n      dataType: \"json\",\n      beforeSend: function beforeSend() {\n        $this.block({\n          message: null,\n          overlayCSS: {\n            background: \"#fff\",\n            opacity: 0.6\n          }\n        });\n      },\n      success: function success(response) {\n        console.log(response);\n\n        if (response.data.loggedin == false) {\n          $(\".autch_form__message\").html(response.data.message);\n        }\n\n        if (response.data.loggedin == true) {\n          document.location.href = ajax_account_object.redirecturl;\n        }\n      },\n      complete: function complete(data) {\n        $this.unblock();\n      }\n    });\n    e.preventDefault();\n  });\n  /**\r\n   * Registration Ajax\r\n   */\n\n  $(document).on(\"submit\", \"#autch_form_registration\", function (e) {\n    var $this = $(this);\n    $.ajax({\n      type: \"POST\",\n      url: ajax_account_object.ajaxurl,\n      data: $this.serialize(),\n      dataType: \"json\",\n      beforeSend: function beforeSend() {\n        $this.block({\n          message: null,\n          overlayCSS: {\n            background: \"#fff\",\n            opacity: 0.6\n          }\n        });\n      },\n      success: function success(response) {\n        if (response.data.loggedin == false) {\n          $(\".autch_form__message\").html(response.data.message);\n        } else {\n          $this.block({\n            message: null,\n            overlayCSS: {\n              background: \"red\",\n              opacity: 0.6\n            }\n          });\n        }\n\n        if (response.data.loggedin == true) {\n          document.location.href = ajax_account_object.redirecturl;\n        }\n      },\n      complete: function complete(data) {\n        $this.unblock();\n      }\n    });\n    e.preventDefault();\n  });\n});\n\n//# sourceURL=webpack:///./assets/js/autch.js?");

/***/ }),

/***/ "./assets/js/subscribe.js":
/*!********************************!*\
  !*** ./assets/js/subscribe.js ***!
  \********************************/
/*! no exports provided */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @babel/runtime/regenerator */ \"./node_modules/@babel/runtime/regenerator/index.js\");\n/* harmony import */ var _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0__);\n/* harmony import */ var _babel_runtime_helpers_asyncToGenerator__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @babel/runtime/helpers/asyncToGenerator */ \"./node_modules/@babel/runtime/helpers/asyncToGenerator.js\");\n/* harmony import */ var _babel_runtime_helpers_asyncToGenerator__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(_babel_runtime_helpers_asyncToGenerator__WEBPACK_IMPORTED_MODULE_1__);\n\n\n// send subscription form\nvar subscription_form = document.querySelector(\".tnp-subscription form\");\nvar formSubscribeMessage = document.querySelector(\".form-subscribe-message\");\nvar loader = document.querySelector(\".your-loader\");\nif (subscription_form) subscription_form.addEventListener(\"submit\", onSubmitForm);\n\nfunction onSubmitForm(_x) {\n  return _onSubmitForm.apply(this, arguments);\n}\n\nfunction _onSubmitForm() {\n  _onSubmitForm = _babel_runtime_helpers_asyncToGenerator__WEBPACK_IMPORTED_MODULE_1___default()( /*#__PURE__*/_babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.mark(function _callee(e) {\n    var fd;\n    return _babel_runtime_regenerator__WEBPACK_IMPORTED_MODULE_0___default.a.wrap(function _callee$(_context) {\n      while (1) {\n        switch (_context.prev = _context.next) {\n          case 0:\n            e.preventDefault();\n            fd = new FormData(e.currentTarget);\n            fd.set(\"action\", \"ajax_subscription\");\n            subscription_form.classList.add(\"is-none\");\n            loader.classList.remove(\"is-none\");\n            _context.prev = 5;\n            _context.next = 8;\n            return fetch(wp.ajaxurl, {\n              method: \"POST\",\n              body: fd\n            });\n\n          case 8:\n            loader.classList.add(\"is-none\");\n            formSubscribeMessage.textContent = \"מזל טוב! נרשמת בהצלחה לניוזלטר. תוך 10 דקות תישלח הודעה למייל שלך.\";\n            _context.next = 15;\n            break;\n\n          case 12:\n            _context.prev = 12;\n            _context.t0 = _context[\"catch\"](5);\n            formSubscribeMessage.textContent = \"טעות! ההרשמה נכשלה. צרו קשר עם בעל האתר.\";\n\n          case 15:\n          case \"end\":\n            return _context.stop();\n        }\n      }\n    }, _callee, null, [[5, 12]]);\n  }));\n  return _onSubmitForm.apply(this, arguments);\n}\n\n//# sourceURL=webpack:///./assets/js/subscribe.js?");

/***/ }),

/***/ "./node_modules/@babel/runtime/helpers/asyncToGenerator.js":
/*!*****************************************************************!*\
  !*** ./node_modules/@babel/runtime/helpers/asyncToGenerator.js ***!
  \*****************************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

eval("function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) {\n  try {\n    var info = gen[key](arg);\n    var value = info.value;\n  } catch (error) {\n    reject(error);\n    return;\n  }\n\n  if (info.done) {\n    resolve(value);\n  } else {\n    Promise.resolve(value).then(_next, _throw);\n  }\n}\n\nfunction _asyncToGenerator(fn) {\n  return function () {\n    var self = this,\n        args = arguments;\n    return new Promise(function (resolve, reject) {\n      var gen = fn.apply(self, args);\n\n      function _next(value) {\n        asyncGeneratorStep(gen, resolve, reject, _next, _throw, \"next\", value);\n      }\n\n      function _throw(err) {\n        asyncGeneratorStep(gen, resolve, reject, _next, _throw, \"throw\", err);\n      }\n\n      _next(undefined);\n    });\n  };\n}\n\nmodule.exports = _asyncToGenerator;\n\n//# sourceURL=webpack:///./node_modules/@babel/runtime/helpers/asyncToGenerator.js?");

/***/ }),

/***/ "./node_modules/@babel/runtime/regenerator/index.js":
/*!**********************************************************!*\
  !*** ./node_modules/@babel/runtime/regenerator/index.js ***!
  \**********************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

eval("module.exports = __webpack_require__(/*! regenerator-runtime */ \"./node_modules/regenerator-runtime/runtime.js\");\n\n\n//# sourceURL=webpack:///./node_modules/@babel/runtime/regenerator/index.js?");

/***/ }),

/***/ "./node_modules/regenerator-runtime/runtime.js":
/*!*****************************************************!*\
  !*** ./node_modules/regenerator-runtime/runtime.js ***!
  \*****************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

eval("/**\n * Copyright (c) 2014-present, Facebook, Inc.\n *\n * This source code is licensed under the MIT license found in the\n * LICENSE file in the root directory of this source tree.\n */\n\nvar runtime = (function (exports) {\n  \"use strict\";\n\n  var Op = Object.prototype;\n  var hasOwn = Op.hasOwnProperty;\n  var undefined; // More compressible than void 0.\n  var $Symbol = typeof Symbol === \"function\" ? Symbol : {};\n  var iteratorSymbol = $Symbol.iterator || \"@@iterator\";\n  var asyncIteratorSymbol = $Symbol.asyncIterator || \"@@asyncIterator\";\n  var toStringTagSymbol = $Symbol.toStringTag || \"@@toStringTag\";\n\n  function define(obj, key, value) {\n    Object.defineProperty(obj, key, {\n      value: value,\n      enumerable: true,\n      configurable: true,\n      writable: true\n    });\n    return obj[key];\n  }\n  try {\n    // IE 8 has a broken Object.defineProperty that only works on DOM objects.\n    define({}, \"\");\n  } catch (err) {\n    define = function(obj, key, value) {\n      return obj[key] = value;\n    };\n  }\n\n  function wrap(innerFn, outerFn, self, tryLocsList) {\n    // If outerFn provided and outerFn.prototype is a Generator, then outerFn.prototype instanceof Generator.\n    var protoGenerator = outerFn && outerFn.prototype instanceof Generator ? outerFn : Generator;\n    var generator = Object.create(protoGenerator.prototype);\n    var context = new Context(tryLocsList || []);\n\n    // The ._invoke method unifies the implementations of the .next,\n    // .throw, and .return methods.\n    generator._invoke = makeInvokeMethod(innerFn, self, context);\n\n    return generator;\n  }\n  exports.wrap = wrap;\n\n  // Try/catch helper to minimize deoptimizations. Returns a completion\n  // record like context.tryEntries[i].completion. This interface could\n  // have been (and was previously) designed to take a closure to be\n  // invoked without arguments, but in all the cases we care about we\n  // already have an existing method we want to call, so there's no need\n  // to create a new function object. We can even get away with assuming\n  // the method takes exactly one argument, since that happens to be true\n  // in every case, so we don't have to touch the arguments object. The\n  // only additional allocation required is the completion record, which\n  // has a stable shape and so hopefully should be cheap to allocate.\n  function tryCatch(fn, obj, arg) {\n    try {\n      return { type: \"normal\", arg: fn.call(obj, arg) };\n    } catch (err) {\n      return { type: \"throw\", arg: err };\n    }\n  }\n\n  var GenStateSuspendedStart = \"suspendedStart\";\n  var GenStateSuspendedYield = \"suspendedYield\";\n  var GenStateExecuting = \"executing\";\n  var GenStateCompleted = \"completed\";\n\n  // Returning this object from the innerFn has the same effect as\n  // breaking out of the dispatch switch statement.\n  var ContinueSentinel = {};\n\n  // Dummy constructor functions that we use as the .constructor and\n  // .constructor.prototype properties for functions that return Generator\n  // objects. For full spec compliance, you may wish to configure your\n  // minifier not to mangle the names of these two functions.\n  function Generator() {}\n  function GeneratorFunction() {}\n  function GeneratorFunctionPrototype() {}\n\n  // This is a polyfill for %IteratorPrototype% for environments that\n  // don't natively support it.\n  var IteratorPrototype = {};\n  IteratorPrototype[iteratorSymbol] = function () {\n    return this;\n  };\n\n  var getProto = Object.getPrototypeOf;\n  var NativeIteratorPrototype = getProto && getProto(getProto(values([])));\n  if (NativeIteratorPrototype &&\n      NativeIteratorPrototype !== Op &&\n      hasOwn.call(NativeIteratorPrototype, iteratorSymbol)) {\n    // This environment has a native %IteratorPrototype%; use it instead\n    // of the polyfill.\n    IteratorPrototype = NativeIteratorPrototype;\n  }\n\n  var Gp = GeneratorFunctionPrototype.prototype =\n    Generator.prototype = Object.create(IteratorPrototype);\n  GeneratorFunction.prototype = Gp.constructor = GeneratorFunctionPrototype;\n  GeneratorFunctionPrototype.constructor = GeneratorFunction;\n  GeneratorFunction.displayName = define(\n    GeneratorFunctionPrototype,\n    toStringTagSymbol,\n    \"GeneratorFunction\"\n  );\n\n  // Helper for defining the .next, .throw, and .return methods of the\n  // Iterator interface in terms of a single ._invoke method.\n  function defineIteratorMethods(prototype) {\n    [\"next\", \"throw\", \"return\"].forEach(function(method) {\n      define(prototype, method, function(arg) {\n        return this._invoke(method, arg);\n      });\n    });\n  }\n\n  exports.isGeneratorFunction = function(genFun) {\n    var ctor = typeof genFun === \"function\" && genFun.constructor;\n    return ctor\n      ? ctor === GeneratorFunction ||\n        // For the native GeneratorFunction constructor, the best we can\n        // do is to check its .name property.\n        (ctor.displayName || ctor.name) === \"GeneratorFunction\"\n      : false;\n  };\n\n  exports.mark = function(genFun) {\n    if (Object.setPrototypeOf) {\n      Object.setPrototypeOf(genFun, GeneratorFunctionPrototype);\n    } else {\n      genFun.__proto__ = GeneratorFunctionPrototype;\n      define(genFun, toStringTagSymbol, \"GeneratorFunction\");\n    }\n    genFun.prototype = Object.create(Gp);\n    return genFun;\n  };\n\n  // Within the body of any async function, `await x` is transformed to\n  // `yield regeneratorRuntime.awrap(x)`, so that the runtime can test\n  // `hasOwn.call(value, \"__await\")` to determine if the yielded value is\n  // meant to be awaited.\n  exports.awrap = function(arg) {\n    return { __await: arg };\n  };\n\n  function AsyncIterator(generator, PromiseImpl) {\n    function invoke(method, arg, resolve, reject) {\n      var record = tryCatch(generator[method], generator, arg);\n      if (record.type === \"throw\") {\n        reject(record.arg);\n      } else {\n        var result = record.arg;\n        var value = result.value;\n        if (value &&\n            typeof value === \"object\" &&\n            hasOwn.call(value, \"__await\")) {\n          return PromiseImpl.resolve(value.__await).then(function(value) {\n            invoke(\"next\", value, resolve, reject);\n          }, function(err) {\n            invoke(\"throw\", err, resolve, reject);\n          });\n        }\n\n        return PromiseImpl.resolve(value).then(function(unwrapped) {\n          // When a yielded Promise is resolved, its final value becomes\n          // the .value of the Promise<{value,done}> result for the\n          // current iteration.\n          result.value = unwrapped;\n          resolve(result);\n        }, function(error) {\n          // If a rejected Promise was yielded, throw the rejection back\n          // into the async generator function so it can be handled there.\n          return invoke(\"throw\", error, resolve, reject);\n        });\n      }\n    }\n\n    var previousPromise;\n\n    function enqueue(method, arg) {\n      function callInvokeWithMethodAndArg() {\n        return new PromiseImpl(function(resolve, reject) {\n          invoke(method, arg, resolve, reject);\n        });\n      }\n\n      return previousPromise =\n        // If enqueue has been called before, then we want to wait until\n        // all previous Promises have been resolved before calling invoke,\n        // so that results are always delivered in the correct order. If\n        // enqueue has not been called before, then it is important to\n        // call invoke immediately, without waiting on a callback to fire,\n        // so that the async generator function has the opportunity to do\n        // any necessary setup in a predictable way. This predictability\n        // is why the Promise constructor synchronously invokes its\n        // executor callback, and why async functions synchronously\n        // execute code before the first await. Since we implement simple\n        // async functions in terms of async generators, it is especially\n        // important to get this right, even though it requires care.\n        previousPromise ? previousPromise.then(\n          callInvokeWithMethodAndArg,\n          // Avoid propagating failures to Promises returned by later\n          // invocations of the iterator.\n          callInvokeWithMethodAndArg\n        ) : callInvokeWithMethodAndArg();\n    }\n\n    // Define the unified helper method that is used to implement .next,\n    // .throw, and .return (see defineIteratorMethods).\n    this._invoke = enqueue;\n  }\n\n  defineIteratorMethods(AsyncIterator.prototype);\n  AsyncIterator.prototype[asyncIteratorSymbol] = function () {\n    return this;\n  };\n  exports.AsyncIterator = AsyncIterator;\n\n  // Note that simple async functions are implemented on top of\n  // AsyncIterator objects; they just return a Promise for the value of\n  // the final result produced by the iterator.\n  exports.async = function(innerFn, outerFn, self, tryLocsList, PromiseImpl) {\n    if (PromiseImpl === void 0) PromiseImpl = Promise;\n\n    var iter = new AsyncIterator(\n      wrap(innerFn, outerFn, self, tryLocsList),\n      PromiseImpl\n    );\n\n    return exports.isGeneratorFunction(outerFn)\n      ? iter // If outerFn is a generator, return the full iterator.\n      : iter.next().then(function(result) {\n          return result.done ? result.value : iter.next();\n        });\n  };\n\n  function makeInvokeMethod(innerFn, self, context) {\n    var state = GenStateSuspendedStart;\n\n    return function invoke(method, arg) {\n      if (state === GenStateExecuting) {\n        throw new Error(\"Generator is already running\");\n      }\n\n      if (state === GenStateCompleted) {\n        if (method === \"throw\") {\n          throw arg;\n        }\n\n        // Be forgiving, per 25.3.3.3.3 of the spec:\n        // https://people.mozilla.org/~jorendorff/es6-draft.html#sec-generatorresume\n        return doneResult();\n      }\n\n      context.method = method;\n      context.arg = arg;\n\n      while (true) {\n        var delegate = context.delegate;\n        if (delegate) {\n          var delegateResult = maybeInvokeDelegate(delegate, context);\n          if (delegateResult) {\n            if (delegateResult === ContinueSentinel) continue;\n            return delegateResult;\n          }\n        }\n\n        if (context.method === \"next\") {\n          // Setting context._sent for legacy support of Babel's\n          // function.sent implementation.\n          context.sent = context._sent = context.arg;\n\n        } else if (context.method === \"throw\") {\n          if (state === GenStateSuspendedStart) {\n            state = GenStateCompleted;\n            throw context.arg;\n          }\n\n          context.dispatchException(context.arg);\n\n        } else if (context.method === \"return\") {\n          context.abrupt(\"return\", context.arg);\n        }\n\n        state = GenStateExecuting;\n\n        var record = tryCatch(innerFn, self, context);\n        if (record.type === \"normal\") {\n          // If an exception is thrown from innerFn, we leave state ===\n          // GenStateExecuting and loop back for another invocation.\n          state = context.done\n            ? GenStateCompleted\n            : GenStateSuspendedYield;\n\n          if (record.arg === ContinueSentinel) {\n            continue;\n          }\n\n          return {\n            value: record.arg,\n            done: context.done\n          };\n\n        } else if (record.type === \"throw\") {\n          state = GenStateCompleted;\n          // Dispatch the exception by looping back around to the\n          // context.dispatchException(context.arg) call above.\n          context.method = \"throw\";\n          context.arg = record.arg;\n        }\n      }\n    };\n  }\n\n  // Call delegate.iterator[context.method](context.arg) and handle the\n  // result, either by returning a { value, done } result from the\n  // delegate iterator, or by modifying context.method and context.arg,\n  // setting context.delegate to null, and returning the ContinueSentinel.\n  function maybeInvokeDelegate(delegate, context) {\n    var method = delegate.iterator[context.method];\n    if (method === undefined) {\n      // A .throw or .return when the delegate iterator has no .throw\n      // method always terminates the yield* loop.\n      context.delegate = null;\n\n      if (context.method === \"throw\") {\n        // Note: [\"return\"] must be used for ES3 parsing compatibility.\n        if (delegate.iterator[\"return\"]) {\n          // If the delegate iterator has a return method, give it a\n          // chance to clean up.\n          context.method = \"return\";\n          context.arg = undefined;\n          maybeInvokeDelegate(delegate, context);\n\n          if (context.method === \"throw\") {\n            // If maybeInvokeDelegate(context) changed context.method from\n            // \"return\" to \"throw\", let that override the TypeError below.\n            return ContinueSentinel;\n          }\n        }\n\n        context.method = \"throw\";\n        context.arg = new TypeError(\n          \"The iterator does not provide a 'throw' method\");\n      }\n\n      return ContinueSentinel;\n    }\n\n    var record = tryCatch(method, delegate.iterator, context.arg);\n\n    if (record.type === \"throw\") {\n      context.method = \"throw\";\n      context.arg = record.arg;\n      context.delegate = null;\n      return ContinueSentinel;\n    }\n\n    var info = record.arg;\n\n    if (! info) {\n      context.method = \"throw\";\n      context.arg = new TypeError(\"iterator result is not an object\");\n      context.delegate = null;\n      return ContinueSentinel;\n    }\n\n    if (info.done) {\n      // Assign the result of the finished delegate to the temporary\n      // variable specified by delegate.resultName (see delegateYield).\n      context[delegate.resultName] = info.value;\n\n      // Resume execution at the desired location (see delegateYield).\n      context.next = delegate.nextLoc;\n\n      // If context.method was \"throw\" but the delegate handled the\n      // exception, let the outer generator proceed normally. If\n      // context.method was \"next\", forget context.arg since it has been\n      // \"consumed\" by the delegate iterator. If context.method was\n      // \"return\", allow the original .return call to continue in the\n      // outer generator.\n      if (context.method !== \"return\") {\n        context.method = \"next\";\n        context.arg = undefined;\n      }\n\n    } else {\n      // Re-yield the result returned by the delegate method.\n      return info;\n    }\n\n    // The delegate iterator is finished, so forget it and continue with\n    // the outer generator.\n    context.delegate = null;\n    return ContinueSentinel;\n  }\n\n  // Define Generator.prototype.{next,throw,return} in terms of the\n  // unified ._invoke helper method.\n  defineIteratorMethods(Gp);\n\n  define(Gp, toStringTagSymbol, \"Generator\");\n\n  // A Generator should always return itself as the iterator object when the\n  // @@iterator function is called on it. Some browsers' implementations of the\n  // iterator prototype chain incorrectly implement this, causing the Generator\n  // object to not be returned from this call. This ensures that doesn't happen.\n  // See https://github.com/facebook/regenerator/issues/274 for more details.\n  Gp[iteratorSymbol] = function() {\n    return this;\n  };\n\n  Gp.toString = function() {\n    return \"[object Generator]\";\n  };\n\n  function pushTryEntry(locs) {\n    var entry = { tryLoc: locs[0] };\n\n    if (1 in locs) {\n      entry.catchLoc = locs[1];\n    }\n\n    if (2 in locs) {\n      entry.finallyLoc = locs[2];\n      entry.afterLoc = locs[3];\n    }\n\n    this.tryEntries.push(entry);\n  }\n\n  function resetTryEntry(entry) {\n    var record = entry.completion || {};\n    record.type = \"normal\";\n    delete record.arg;\n    entry.completion = record;\n  }\n\n  function Context(tryLocsList) {\n    // The root entry object (effectively a try statement without a catch\n    // or a finally block) gives us a place to store values thrown from\n    // locations where there is no enclosing try statement.\n    this.tryEntries = [{ tryLoc: \"root\" }];\n    tryLocsList.forEach(pushTryEntry, this);\n    this.reset(true);\n  }\n\n  exports.keys = function(object) {\n    var keys = [];\n    for (var key in object) {\n      keys.push(key);\n    }\n    keys.reverse();\n\n    // Rather than returning an object with a next method, we keep\n    // things simple and return the next function itself.\n    return function next() {\n      while (keys.length) {\n        var key = keys.pop();\n        if (key in object) {\n          next.value = key;\n          next.done = false;\n          return next;\n        }\n      }\n\n      // To avoid creating an additional object, we just hang the .value\n      // and .done properties off the next function object itself. This\n      // also ensures that the minifier will not anonymize the function.\n      next.done = true;\n      return next;\n    };\n  };\n\n  function values(iterable) {\n    if (iterable) {\n      var iteratorMethod = iterable[iteratorSymbol];\n      if (iteratorMethod) {\n        return iteratorMethod.call(iterable);\n      }\n\n      if (typeof iterable.next === \"function\") {\n        return iterable;\n      }\n\n      if (!isNaN(iterable.length)) {\n        var i = -1, next = function next() {\n          while (++i < iterable.length) {\n            if (hasOwn.call(iterable, i)) {\n              next.value = iterable[i];\n              next.done = false;\n              return next;\n            }\n          }\n\n          next.value = undefined;\n          next.done = true;\n\n          return next;\n        };\n\n        return next.next = next;\n      }\n    }\n\n    // Return an iterator with no values.\n    return { next: doneResult };\n  }\n  exports.values = values;\n\n  function doneResult() {\n    return { value: undefined, done: true };\n  }\n\n  Context.prototype = {\n    constructor: Context,\n\n    reset: function(skipTempReset) {\n      this.prev = 0;\n      this.next = 0;\n      // Resetting context._sent for legacy support of Babel's\n      // function.sent implementation.\n      this.sent = this._sent = undefined;\n      this.done = false;\n      this.delegate = null;\n\n      this.method = \"next\";\n      this.arg = undefined;\n\n      this.tryEntries.forEach(resetTryEntry);\n\n      if (!skipTempReset) {\n        for (var name in this) {\n          // Not sure about the optimal order of these conditions:\n          if (name.charAt(0) === \"t\" &&\n              hasOwn.call(this, name) &&\n              !isNaN(+name.slice(1))) {\n            this[name] = undefined;\n          }\n        }\n      }\n    },\n\n    stop: function() {\n      this.done = true;\n\n      var rootEntry = this.tryEntries[0];\n      var rootRecord = rootEntry.completion;\n      if (rootRecord.type === \"throw\") {\n        throw rootRecord.arg;\n      }\n\n      return this.rval;\n    },\n\n    dispatchException: function(exception) {\n      if (this.done) {\n        throw exception;\n      }\n\n      var context = this;\n      function handle(loc, caught) {\n        record.type = \"throw\";\n        record.arg = exception;\n        context.next = loc;\n\n        if (caught) {\n          // If the dispatched exception was caught by a catch block,\n          // then let that catch block handle the exception normally.\n          context.method = \"next\";\n          context.arg = undefined;\n        }\n\n        return !! caught;\n      }\n\n      for (var i = this.tryEntries.length - 1; i >= 0; --i) {\n        var entry = this.tryEntries[i];\n        var record = entry.completion;\n\n        if (entry.tryLoc === \"root\") {\n          // Exception thrown outside of any try block that could handle\n          // it, so set the completion value of the entire function to\n          // throw the exception.\n          return handle(\"end\");\n        }\n\n        if (entry.tryLoc <= this.prev) {\n          var hasCatch = hasOwn.call(entry, \"catchLoc\");\n          var hasFinally = hasOwn.call(entry, \"finallyLoc\");\n\n          if (hasCatch && hasFinally) {\n            if (this.prev < entry.catchLoc) {\n              return handle(entry.catchLoc, true);\n            } else if (this.prev < entry.finallyLoc) {\n              return handle(entry.finallyLoc);\n            }\n\n          } else if (hasCatch) {\n            if (this.prev < entry.catchLoc) {\n              return handle(entry.catchLoc, true);\n            }\n\n          } else if (hasFinally) {\n            if (this.prev < entry.finallyLoc) {\n              return handle(entry.finallyLoc);\n            }\n\n          } else {\n            throw new Error(\"try statement without catch or finally\");\n          }\n        }\n      }\n    },\n\n    abrupt: function(type, arg) {\n      for (var i = this.tryEntries.length - 1; i >= 0; --i) {\n        var entry = this.tryEntries[i];\n        if (entry.tryLoc <= this.prev &&\n            hasOwn.call(entry, \"finallyLoc\") &&\n            this.prev < entry.finallyLoc) {\n          var finallyEntry = entry;\n          break;\n        }\n      }\n\n      if (finallyEntry &&\n          (type === \"break\" ||\n           type === \"continue\") &&\n          finallyEntry.tryLoc <= arg &&\n          arg <= finallyEntry.finallyLoc) {\n        // Ignore the finally entry if control is not jumping to a\n        // location outside the try/catch block.\n        finallyEntry = null;\n      }\n\n      var record = finallyEntry ? finallyEntry.completion : {};\n      record.type = type;\n      record.arg = arg;\n\n      if (finallyEntry) {\n        this.method = \"next\";\n        this.next = finallyEntry.finallyLoc;\n        return ContinueSentinel;\n      }\n\n      return this.complete(record);\n    },\n\n    complete: function(record, afterLoc) {\n      if (record.type === \"throw\") {\n        throw record.arg;\n      }\n\n      if (record.type === \"break\" ||\n          record.type === \"continue\") {\n        this.next = record.arg;\n      } else if (record.type === \"return\") {\n        this.rval = this.arg = record.arg;\n        this.method = \"return\";\n        this.next = \"end\";\n      } else if (record.type === \"normal\" && afterLoc) {\n        this.next = afterLoc;\n      }\n\n      return ContinueSentinel;\n    },\n\n    finish: function(finallyLoc) {\n      for (var i = this.tryEntries.length - 1; i >= 0; --i) {\n        var entry = this.tryEntries[i];\n        if (entry.finallyLoc === finallyLoc) {\n          this.complete(entry.completion, entry.afterLoc);\n          resetTryEntry(entry);\n          return ContinueSentinel;\n        }\n      }\n    },\n\n    \"catch\": function(tryLoc) {\n      for (var i = this.tryEntries.length - 1; i >= 0; --i) {\n        var entry = this.tryEntries[i];\n        if (entry.tryLoc === tryLoc) {\n          var record = entry.completion;\n          if (record.type === \"throw\") {\n            var thrown = record.arg;\n            resetTryEntry(entry);\n          }\n          return thrown;\n        }\n      }\n\n      // The context.catch method must only be called with a location\n      // argument that corresponds to a known catch block.\n      throw new Error(\"illegal catch attempt\");\n    },\n\n    delegateYield: function(iterable, resultName, nextLoc) {\n      this.delegate = {\n        iterator: values(iterable),\n        resultName: resultName,\n        nextLoc: nextLoc\n      };\n\n      if (this.method === \"next\") {\n        // Deliberately forget the last sent value so that we don't\n        // accidentally pass it on to the delegate.\n        this.arg = undefined;\n      }\n\n      return ContinueSentinel;\n    }\n  };\n\n  // Regardless of whether this script is executing as a CommonJS module\n  // or not, return the runtime object so that we can declare the variable\n  // regeneratorRuntime in the outer scope, which allows this module to be\n  // injected easily by `bin/regenerator --include-runtime script.js`.\n  return exports;\n\n}(\n  // If this script is executing as a CommonJS module, use module.exports\n  // as the regeneratorRuntime namespace. Otherwise create a new empty\n  // object. Either way, the resulting object will be used to initialize\n  // the regeneratorRuntime variable at the top of this file.\n   true ? module.exports : undefined\n));\n\ntry {\n  regeneratorRuntime = runtime;\n} catch (accidentalStrictMode) {\n  // This module should not be running in strict mode, so the above\n  // assignment should always work unless something is misconfigured. Just\n  // in case runtime.js accidentally runs in strict mode, we can escape\n  // strict mode using a global Function call. This could conceivably fail\n  // if a Content Security Policy forbids using Function, but in that case\n  // the proper solution is to fix the accidental strict mode problem. If\n  // you've misconfigured your bundler to force strict mode and applied a\n  // CSP to forbid Function, and you're not willing to fix either of those\n  // problems, please detail your unique predicament in a GitHub issue.\n  Function(\"r\", \"regeneratorRuntime = r\")(runtime);\n}\n\n\n//# sourceURL=webpack:///./node_modules/regenerator-runtime/runtime.js?");

/***/ })

/******/ });